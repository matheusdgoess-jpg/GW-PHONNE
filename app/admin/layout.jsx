import './admin.css';

export const metadata = {
  title: 'Painel da Loja · GW Phone',
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return children;
}
