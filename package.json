{
  "name": "gw-phonne",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --check script.js && node --check login.js && node --check admin.js",
    "dev": "npx serve .",
    "deploy": "vercel --prod"
  }
}
